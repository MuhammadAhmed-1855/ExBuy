// import { render, screen } from '@testing-library/react';
// import Header from './components/headers/Header';

// test('Logo Check', () => {
//     render(<Header />);
//     const linkElement = screen.getByTestId("LogoChecker");
//     expect(linkElement).toBeInTheDocument("ExBuy");
// });

// Tried running tests but following error occurs:
// SyntaxError: Cannot use import statement outside a module